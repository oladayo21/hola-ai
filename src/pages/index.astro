---
import Base from '../layouts/Base.astro';
---

<Base title="Hola AI" description="Ressourcen für KI-gestütztes Programmieren. Workshops, Prompts und Tutorials.">
  <!-- Hero -->
  <section class="hero">
    <div class="hero-grid"></div>
    <div class="container hero-container">
      <h1 class="hero-title">Hola AI</h1>
      <p class="hero-subtitle">
        Ressourcen für KI-gestütztes Programmieren.<br/>
        Workshops, Prompts und Tutorials für den Entwickler-Alltag.
      </p>
      <div class="hero-actions">
        <a href="/workshops" class="btn btn-primary">Workshops</a>
        <a href="/prompts" class="btn btn-secondary">Prompts</a>
        <a href="/blog" class="btn btn-secondary">Blog</a>
      </div>
    </div>
  </section>

  <!-- Terminal Demo -->
  <section class="terminal-demo">
    <div class="container">
      <div class="terminal-window">
        <div class="terminal-chrome">
          <span class="terminal-dot terminal-dot--red"></span>
          <span class="terminal-dot terminal-dot--yellow"></span>
          <span class="terminal-dot terminal-dot--green"></span>
          <span class="terminal-title">Terminal</span>
        </div>
        <div class="terminal-content">
          <div class="terminal-line">
            <span class="terminal-prompt">~/project $</span>
            <span class="terminal-cmd">ai "Erkläre mir diese Funktion"</span>
          </div>
          <div class="terminal-output">
            <span class="terminal-comment"># Die Funktion `parseConfig` liest die Konfigurationsdatei</span><br/>
            <span class="terminal-comment"># und validiert die Eingaben gegen das Schema...</span>
          </div>
          <div class="terminal-line">
            <span class="terminal-prompt">~/project $</span>
            <span class="terminal-cmd">ai "Schreibe Tests für auth.ts"</span>
          </div>
          <div class="terminal-output">
            <span class="terminal-success">✓</span> <span class="terminal-file">auth.test.ts</span> erstellt<br/>
            <span class="terminal-muted">  → 12 Test-Cases für login(), logout(), refresh()</span>
          </div>
          <div class="terminal-line">
            <span class="terminal-prompt">~/project $</span>
            <span class="terminal-cmd">ai review ./src --fix</span>
          </div>
          <div class="terminal-output">
            <span class="terminal-muted">Analysiere 24 Dateien...</span><br/>
            <span class="terminal-warning">⚠</span> <span class="terminal-file">api/handler.ts:42</span> — Fehlende Fehlerbehandlung<br/>
            <span class="terminal-success">✓</span> Auto-fix angewendet
          </div>
          <div class="terminal-line terminal-line--active">
            <span class="terminal-prompt">~/project $</span>
            <span class="terminal-cursor"></span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section class="about">
    <div class="container">
      <div class="about-content">
        <p class="about-lead">
          Die neuen AI-Coding-Tools sind mächtig, aber nicht immer intuitiv.
        </p>
        <p>
          Hola AI sammelt praktisches Wissen: Was funktioniert wirklich im Alltag?
          Welche Prompts liefern gute Ergebnisse? Wo sind die Grenzen?
          Keine Marketing-Versprechen — nur ehrliche Erfahrungen aus der Praxis.
        </p>
      </div>
    </div>
  </section>
</Base>

<style>
  /* Hero */
  .hero {
    position: relative;
    padding: var(--space-32) 0 var(--space-20);
    overflow: hidden;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 1px 1px, var(--color-border-subtle) 1px, transparent 0);
    background-size: 32px 32px;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, black 20%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, black 20%, transparent 70%);
    pointer-events: none;
  }

  /* Terminal Demo */
  .terminal-demo {
    padding: var(--space-8) 0 var(--space-16);
  }

  .terminal-window {
    max-width: 680px;
    margin: 0 auto;
    border-radius: 6px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }

  .terminal-chrome {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;
    background: var(--color-bg-elevated);
  }

  .terminal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .terminal-dot--red { background: #ff5f57; }
  .terminal-dot--yellow { background: #febc2e; }
  .terminal-dot--green { background: #28c840; }

  .terminal-title {
    margin-left: auto;
    margin-right: auto;
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    font-family: var(--font-mono);
  }

  .terminal-content {
    background: #0a0a0a;
    padding: var(--space-5);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    line-height: 1.7;
  }

  .terminal-line {
    margin-bottom: var(--space-1);
  }

  .terminal-prompt {
    color: var(--color-accent);
    margin-right: var(--space-2);
  }

  .terminal-cmd {
    color: var(--color-text);
  }

  .terminal-output {
    color: var(--color-text-secondary);
    padding-left: var(--space-4);
    margin-bottom: var(--space-3);
  }

  .terminal-comment {
    color: var(--color-text-muted);
  }

  .terminal-success {
    color: #28c840;
  }

  .terminal-warning {
    color: #febc2e;
  }

  .terminal-file {
    color: var(--color-accent);
  }

  .terminal-muted {
    color: var(--color-text-muted);
  }

  .terminal-cursor {
    display: inline-block;
    width: 8px;
    height: 16px;
    background: var(--color-accent);
    animation: blink 1s step-end infinite;
    vertical-align: middle;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .hero-container {
    position: relative;
    text-align: center;
    max-width: 640px;
  }

  .hero-title {
    font-size: clamp(3.5rem, 10vw, 6rem);
    font-weight: 500;
    letter-spacing: -0.04em;
    margin-bottom: var(--space-6);
    background: linear-gradient(180deg, var(--color-text) 0%, var(--color-text-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-10);
  }

  .hero-actions {
    display: flex;
    gap: var(--space-3);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 120px;
    padding: var(--space-2) var(--space-5);
    font-size: var(--text-sm);
    font-weight: 500;
    border-radius: 2px;
    transition: all var(--transition-fast);
  }

  .btn-primary {
    background: var(--color-accent);
    color: #fff;
  }

  .btn-primary:hover {
    background: var(--color-accent-hover);
    color: #fff;
  }

  .btn-secondary {
    background: var(--color-bg-muted);
    color: var(--color-text);
  }

  .btn-secondary:hover {
    background: var(--color-bg-elevated);
  }

  /* Features */
  .features {
    padding: var(--space-12) 0 var(--space-16);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--color-border-subtle);
    border-radius: 0;
    overflow: hidden;
  }

  .feature-card {
    background: var(--color-bg-subtle);
    padding: var(--space-6);
    transition: background var(--transition-fast);
  }

  .feature-card:hover {
    background: var(--color-bg-muted);
  }

  .feature-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-3);
  }

  .feature-icon {
    color: var(--color-accent);
  }

  .feature-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-text);
  }

  .feature-card p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
  }

  /* About */
  .about {
    padding: var(--space-20) 0 var(--space-24);
    text-align: center;
  }

  .about-content {
    max-width: 640px;
    margin: 0 auto;
  }

  .about-lead {
    font-size: var(--text-2xl);
    color: var(--color-text);
    font-weight: 500;
    margin-bottom: var(--space-5);
    letter-spacing: -0.02em;
    line-height: var(--leading-snug);
  }

  .about p {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
  }

  /* Mobile */
  @media (max-width: 768px) {
    .hero {
      padding: var(--space-20) 0 var(--space-12);
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .btn {
      width: 100%;
      max-width: 200px;
      justify-content: center;
    }
  }
</style>
